using System;

namespace FruitVegt
{
    class Program
    {
        static void Main(string[] args)
        {
            int number = int.Parse(Console.ReadLine());

            bool bValid = number >= 100 && number <= 200 || number == 0;

            if (!bValid)
            {
                Console.WriteLine("invalid");
            }
        }
    }
}
